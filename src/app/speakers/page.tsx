import Title from "@/ui/title/title";
import styles from "./page.module.scss";

export default function Page(){

    return(
        <main>
            <Title text="SPEAKERS"/>
        </main>
    );
}